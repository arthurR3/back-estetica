import cron from 'node-cron'
import { getDateNotification } from '../controllers/dates.controller.js'

export const jobsDates = () => {
   // cron.schedule("*/5 * * * *", async () => {
/*         console.log('Enviando Notificacion al usuario')
        try {
            await getDateNotification()
        } catch (error) {
            console.log('Error al ejecutar el job ', error);
        }
    })  */
}